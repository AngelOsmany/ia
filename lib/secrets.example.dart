// Ejemplo de archivo de secretos (NO incluir claves reales aquí)
// - Copia este archivo a `lib/secrets.dart` y añade `lib/secrets.dart` a .gitignore
// - Nunca subas `lib/secrets.dart` al repositorio.

class Secrets {
  // Rellena los valores en tu archivo `lib/secrets.dart` local (no versionado)
  static const String geminiKey = '';
  static const String cohereKey = '';
  static const String hfKey = '';
  static const String stabilityKey = '';
}
